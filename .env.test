# Test environment variables for email functionality
# These are dummy values for testing - replace with real values for production

# SMTP Configuration (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@example.com
SMTP_PASS=test_app_password

# Email Configuration
ADMIN_EMAIL=admin@example.com
COMPANY_NAME=Mantra Group
FROM_NAME=Website Contact

# Frontend Configuration
VITE_EMAIL_API_ENDPOINT=http://localhost:3001/api/send-email
VITE_ADMIN_EMAIL=admin@example.com
VITE_COMPANY_NAME=Mantra Group
VITE_SITE_URL=http://localhost:5173